import { SignUp } from '@clerk/nextjs';
export default function Page(){const ready=Boolean(process.env.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY&&process.env.CLERK_SECRET_KEY);return <main id="conteudo"><section className="section"><div className="shell" style={{display:'grid',placeItems:'center'}}>{ready?<SignUp/>:<div className="track-card" style={{maxWidth:560}}><span className="badge">Staging</span><h1 style={{fontSize:'1.7rem'}}>Cadastro ainda não provisionado</h1><p>A interface está preparada, mas o cadastro só será ativado após configurar Clerk no ambiente de staging.</p></div>}</div></section></main>}
