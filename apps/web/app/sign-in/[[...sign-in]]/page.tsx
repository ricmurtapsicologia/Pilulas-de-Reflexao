import { SignIn } from '@clerk/nextjs';
export default function Page(){const ready=Boolean(process.env.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY&&process.env.CLERK_SECRET_KEY);return <main id="conteudo"><section className="section"><div className="shell" style={{display:'grid',placeItems:'center'}}>{ready?<SignIn/>:<div className="track-card" style={{maxWidth:560}}><span className="badge">Staging</span><h1 style={{fontSize:'1.7rem'}}>Autenticação ainda não provisionada</h1><p>O fluxo está implementado, mas depende das chaves Clerk no projeto de staging. A biblioteca livre continua utilizável sem conta.</p></div>}</div></section></main>}
